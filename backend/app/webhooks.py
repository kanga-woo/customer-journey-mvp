def normalize_mailchimp(payload):
# return a list of EventPayload-like dicts
events = []
# Add examples depending on payload structure
return events


def normalize_zendesk_ticket(payload):
events = []
# extract ticket text, customer id, severity
return events
